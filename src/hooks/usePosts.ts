// Compatibility shim for existing code
import { useSocialPosts, useSocialPostComments, useAddSocialComment } from '@/hooks/useSocialFeed';

export { useSocialPosts as usePosts, useSocialPostComments as usePostComments, useAddSocialComment as useAddComment };